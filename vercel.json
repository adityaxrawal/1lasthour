{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" == \"prod-fe\" ]; then exit 1; else exit 0; fi",
  "installCommand": "corepack enable pnpm && pnpm install",
  "buildCommand": "pnpm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
